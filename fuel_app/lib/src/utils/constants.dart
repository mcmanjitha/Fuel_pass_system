import 'package:flutter/material.dart';

const appBarBackgroundColor = LinearGradient(
  colors: [Color.fromARGB(255, 242, 243, 243), Color.fromARGB(255, 94, 93, 93)],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

const loginLabelTextStyle = TextStyle(fontSize: 16.0, color: maroon);

const appBarLabelStyle = TextStyle(fontSize: 12.0, color: maroon);

const maroon = Color.fromARGB(255, 101, 1, 33);

const grey = Color.fromARGB(255, 150, 149, 149);
const lightgrey = Color.fromARGB(255, 230, 230, 230);

const white = Color.fromARGB(255, 255, 255, 255);
